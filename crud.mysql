#mysql -u root -p

CREATE DATABASE IF NOT EXISTS CRUD;
GRANT ALL ON CRUD.* TO 'crud'@'localhost' IDENTIFIED BY 'crud';
USE CRUD;
CREATE TABLE IF NOT EXISTS Crud (id INT PRIMARY KEY AUTO_INCREMENT, Nombre VARCHAR(100) NOT NULL, Apellidos VARCHAR(100) NOT NULL, DNI VARCHAR(9), Vivienda VARCHAR(100));
